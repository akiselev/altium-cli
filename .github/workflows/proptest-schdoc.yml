name: Proptest SchDoc

on:
  push:
    branches:
      - main
      - master
    tags:
      - "v*"
  pull_request:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  proptest-schdoc:
    name: SchDoc fixture-backed proptests
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2

      - name: Fetch SchDoc fixtures
        run: |
          set -euo pipefail
          mkdir -p data
          rm -rf data/schdoc
          git clone --depth 1 https://github.com/akiselev/altium-cli-test-schdoc data/schdoc

      - name: Run altium-format SchDoc proptests
        run: cargo test -p altium-format --lib prop_schdoc --verbose

      - name: Run altium-format-ops SchDoc proptests
        run: cargo test -p altium-format-ops --test executor_schdoc_proptest --verbose
