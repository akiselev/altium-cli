name: CI

on:
  push:
    branches:
      - main
      - master
  pull_request:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      # TODO: Add checkout steps for autopcb and solverang sibling repos
      # once GitHub repo structure is finalized. The altium-cli binary
      # depends on autopcb crates via cross-repo path deps.

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2

      - name: Build workspace
        run: cargo build --workspace --verbose

      - name: Run unit tests (no fixtures, no proptests)
        run: cargo test --workspace --all-targets --verbose
