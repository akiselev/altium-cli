name: Fixtures Integration

on:
  push:
    branches:
      - main
      - master
    tags:
      - "v*"
  pull_request:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  fixture-integration:
    name: Cross-document integration tests
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2

      - name: Fetch SchLib fixtures
        run: |
          set -euo pipefail
          mkdir -p data
          rm -rf data/schlib
          git clone --depth 1 https://github.com/akiselev/altium-cli-test-schlib data/schlib

      - name: Fetch SchDoc fixtures
        run: |
          set -euo pipefail
          rm -rf data/schdoc
          git clone --depth 1 https://github.com/akiselev/altium-cli-test-schdoc data/schdoc

      - name: Run executor integration tests
        run: cargo test -p altium-format-ops --test executor_integration --features test-fixtures --verbose
