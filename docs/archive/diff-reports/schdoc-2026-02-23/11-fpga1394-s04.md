# Roundtrip Diff Report: jhu-cisst_FPGA1394__S04.SchDoc

## Summary
- **File size**: 1,856,000 bytes original -> 1,863,680 bytes roundtripped (+7,680 bytes / +0.4%)
- **Save-as result**: Success
- **Streams differing**: 3 (/Additional, /FileHeader, /Storage)
- **Streams identical**: 0

## Stream-by-Stream Analysis

### /Additional
- **Status**: DIFFERS
- **Size change**: 75 bytes -> 84 bytes (+9 bytes)
- **Diff category**: Default value injection
- **Details**: 1 block differs (block[0]). The roundtrip adds `Weight=0` to the header record:
  - Original: `|HEADER=Protel for Windows - Schematic Capture Binary File Version 5.0`
  - Roundtrip: `|HEADER=Protel for Windows - Schematic Capture Binary File Version 5.0|Weight=0`

### /FileHeader
- **Status**: DIFFERS
- **Size change**: 1,742,059 bytes -> 1,741,989 bytes (-70 bytes)
- **Diff category**: Case normalization, parameter reordering, font reordering
- **Details**: 11,047 blocks differ. This is another legacy-format file with ALL-UPPERCASE parameter keys. The patterns match file 10 closely:

  **1. Case normalization (ALL blocks):**
  All ~11,047 blocks have parameter keys converted from uppercase to canonical mixed-case. Unlike file 10, this file already has `MinorVersion` and `UniqueID` in the header, so the net size change is only -70 bytes (case normalization makes keys slightly different lengths in a few cases, but mostly the same).

  **2. Header block (block[0]):**
  Simple case change only:
  - Original: `...WEIGHT=11046|MINORVERSION=2|UNIQUEID=KRRULPGW`
  - Roundtrip: `...Weight=11046|MinorVersion=2|UniqueID=KRRULPGW`

  **3. Font parameter reordering (block[1] -- RECORD=31):**
  Same pattern as all other files: font fields reordered from `SizeN|FontNameN` to `FontNameN|SizeN`. Also `Bold4=T|Italic4=T` reordered to `Italic4=T|Bold4=T` in the original but `Bold4=T|Italic4=T` in roundtrip. Sheet properties reordered to canonical order. `SystemFont`, `UseMBCS`, `IsBOC`, `AreaColor` moved to end.

  **4. Template records (blocks 2-60+):**
  RECORD=39 (Template), RECORD=4 (Label), RECORD=6 (Polyline) -- all template-related records have simple case normalization.

  **5. Component and child records:**
  RECORD=1 (Component), RECORD=2 (Pin), RECORD=34 (Designator), RECORD=41 (Parameter), RECORD=44 (Component Implementation List), RECORD=45 (Implementation), RECORD=46 (Implementation Map), RECORD=48 (Implementation PinMap) -- all case-normalized.

  **6. Junction records (end of file):**
  RECORD=29 (Junction) records at the end of the file also case-normalized.

### /Storage
- **Status**: DIFFERS
- **Size change**: 96,366 bytes -> 97,621 bytes (+1,255 bytes)
- **Diff category**: Case normalization + binary differences
- **Details**: 2 blocks differ:
  - **block[0]**: Text header case-normalized: `WEIGHT=1` -> `Weight=1`
  - **block[1]**: Binary block size changed from 96,328 to 97,583 bytes (+1,255 bytes). This stream contains icon/model storage data. The size increase suggests the embedded data is re-serialized with slightly different packing.

## Diff Categories Found

1. **Parameter reordering** -- Font fields and sheet properties reordered in RECORD=31. Same canonical ordering as files 08/09/10.
2. **Font parameter reordering** -- `FontNameN|SizeN` ordering (our canonical) vs original `SizeN|FontNameN`.
3. **Default value injection** -- `Weight=0` added to /Additional header.
4. **Case normalization** -- ALL-UPPERCASE keys normalized to mixed-case across all ~11,047 blocks. This is the dominant diff type for this file.
5. **Binary differences** -- /Storage binary block increased by 1,255 bytes.

## Fidelity Assessment

**BENIGN**: All text-mode differences are case normalization and parameter reordering, both of which are semantically invisible to Altium (case-insensitive key lookup, order-independent parsing). The /Storage binary size increase warrants investigation but does not indicate data loss.

Unlike file 10 (which had duplicate parameter removal causing a -3% size decrease), this file has no duplicate parameters, so the net file size increase of +0.4% comes primarily from the /Storage binary size increase and the `Weight=0` addition.

## Impact on File Format Support

**What's working well:**
- Complete parsing of another legacy ALL-UPPERCASE format file
- All record types fully supported: 1, 2, 4, 6, 29, 31, 34, 39, 41, 44, 45, 46, 48
- All parameter values preserved correctly (coordinates, colors, font IDs, text strings, file paths, etc.)
- Case normalization is consistent and predictable (upgrade to modern format)
- Header fields preserved correctly (Weight, MinorVersion, UniqueID all present in both)

**What needs improvement:**
- Parameter ordering in RECORD=31 (font fields, sheet properties) could match Altium's native order
- /Storage binary differences (+1,255 bytes) need investigation -- may be related to embedded icon/model data re-serialization or CFB sector allocation differences
- The `Weight=0` injection in /Additional should be verified against Altium's behavior for files with empty /Additional streams
