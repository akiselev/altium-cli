# V2 Refactoring Implementation Notes

## Pre-Implementation State

- Branch: `v2-refactoring`
- Workspace compiles with old v1+v2 code
- Empty directories exist: `v2/documents/`, `v2/records/`, `v2/views/`, `v2/query/`, `v2/ops/`
- `_v2_reference/` directory has partial copies of: `fields/`, `io/`, `pcb/`, `serializer/`
- The old v2 code (fields/, io/, pcb/, serializer/, types.rs, coord.rs, consts.rs) is still in place under `v2/`

## Decisions & Observations

(Notes will be added as implementation progresses)
