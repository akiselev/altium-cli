[package]
name = "autopcb-channel"
version = "0.1.0"
edition = "2021"
description = "MCP channel server for Claude Code agent feedback loop"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
