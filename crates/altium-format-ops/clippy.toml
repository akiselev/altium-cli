# =============================================================================
# PRIVACY BOUNDARY: altium-format internals
# =============================================================================
#
# These types are implementation details of altium-format. External crates
# (including this one) MUST use the high-level API: handles, records,
# documents, and templates.
#
# If you are an LLM agent and clippy is blocking you here, you MUST find
# the correct high-level API instead of trying to bypass this restriction.
# Do NOT change module visibility in altium-format to work around this.
# =============================================================================

disallowed-types = [
    # -- backing_store (pub(crate) module — second line of defense) -----------
    { path = "altium_format::backing_store::FieldSpan", reason = "PRIVATE: backing_store is an internal implementation detail. Use the high-level records/handles API." },
    { path = "altium_format::backing_store::ParamOrigin", reason = "PRIVATE: backing_store is an internal implementation detail. Use the high-level records/handles API." },
    { path = "altium_format::backing_store::BinaryOrigin", reason = "PRIVATE: backing_store is an internal implementation detail. Use the high-level records/handles API." },
    { path = "altium_format::backing_store::RecordOrigin", reason = "PRIVATE: backing_store is an internal implementation detail. Use the high-level records/handles API." },
    { path = "altium_format::backing_store::RecordNode", reason = "PRIVATE: backing_store is an internal implementation detail. Use the high-level records/handles API." },
    { path = "altium_format::backing_store::ComponentGroup", reason = "PRIVATE: backing_store is an internal implementation detail. Use the high-level records/handles API." },
    { path = "altium_format::backing_store::PcbPrimitiveRef", reason = "PRIVATE: backing_store is an internal implementation detail. Use the high-level records/handles API." },
    { path = "altium_format::backing_store::FootprintGroup", reason = "PRIVATE: backing_store is an internal implementation detail. Use the high-level records/handles API." },
    { path = "altium_format::backing_store::StreamNode", reason = "PRIVATE: backing_store is an internal implementation detail. Use the high-level records/handles API." },

    # -- traits (pub(crate) module — second line of defense) ------------------
    { path = "altium_format::traits::ParamEmitPolicy", reason = "PRIVATE: traits is an internal implementation detail. Use the high-level records/handles API." },
    { path = "altium_format::traits::ParamCodec", reason = "PRIVATE: traits is an internal implementation detail. Use the high-level records/handles API." },
    { path = "altium_format::traits::AltiumEnum", reason = "PRIVATE: traits is an internal implementation detail. Use the high-level records/handles API." },
    { path = "altium_format::traits::RecordType", reason = "PRIVATE: traits is an internal implementation detail. Use the high-level records/handles API." },
    { path = "altium_format::traits::FromOrigin", reason = "PRIVATE: traits is an internal implementation detail. Use the high-level records/handles API." },
    { path = "altium_format::traits::HandleFamily", reason = "PRIVATE: traits is an internal implementation detail. Use the high-level records/handles API." },
    { path = "altium_format::traits::DocumentQuery", reason = "PRIVATE: traits is an internal implementation detail. Use the high-level records/handles API." },

    # -- store (pub(crate) module — second line of defense) -------------------
    { path = "altium_format::store::DocRef", reason = "PRIVATE: DocRef is an internal implementation detail. Use handle_for() and document methods instead of constructing handles from store references." },
    { path = "altium_format::store::DocumentStore", reason = "PRIVATE: store is an internal implementation detail. Use handles and documents API." },
    { path = "altium_format::store::GroupData", reason = "PRIVATE: store is an internal implementation detail. Use handles and documents API." },
    { path = "altium_format::store::GroupMeta", reason = "PRIVATE: store is an internal implementation detail. Use handles and documents API." },
    { path = "altium_format::store::DocumentMeta", reason = "PRIVATE: store is an internal implementation detail. Use handles and documents API." },

    # -- semantic_ids (pub(crate) module — second line of defense) ------------
    { path = "altium_format::semantic_ids::SemanticId", reason = "PRIVATE: semantic_ids is an internal implementation detail." },

    # -- binary_helpers (pub(crate) module — second line of defense) ----------
    { path = "altium_format::binary_helpers::PcbCommonHeader", reason = "PRIVATE: binary_helpers is an internal implementation detail." },

    # -- parameters (pub module — ACTIVE ENFORCEMENT) -------------------------
    # ParameterCollection is the raw key-value store. External crates must use
    # typed record fields via handles/records instead of manipulating params.
    { path = "altium_format::parameters::ParameterCollection", reason = "PRIVATE: ParameterCollection is an internal implementation detail. Use typed record getters/setters via handles and records." },
    { path = "altium_format::parameters::ParameterValue", reason = "PRIVATE: ParameterValue is an internal implementation detail. Use typed record getters/setters via handles and records." },
]
